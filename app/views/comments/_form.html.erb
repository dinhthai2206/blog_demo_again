<%= form_for comment, remote: true do |f| %>
  <div class="field">
    <%= f.hidden_field :micropost_id, value: "#{micropost.id}" %>

    <%= f.text_field :content, placeholder: "Your comment..." %>
    <%= f.submit "Comment", class: "btn btn-primary" %>
  </div>
<% end %>
