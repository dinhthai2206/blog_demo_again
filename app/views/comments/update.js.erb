var comment_content = '<%= escape_javascript(render('comments/comment', comment: @comment)) %>';
$('#comment-<%= @comment.id %>').html($(comment_content).html());
$('#editForm<%= @comment.id %>').collapse('hide')
